[phases.setup]
nixPkgs = ["php83", "composer", "nodejs", "npm"]

[phases.install]
cmds = [
    "composer install --no-dev --optimize-autoloader",
    "npm install",
    "npm run build"
]

[phases.build]
cmds = [
    "php bin/console cache:clear --env=prod",
    "php bin/console doctrine:migrations:migrate --no-interaction"
]

[start]
cmd = "php -S 0.0.0.0:$PORT -t web"